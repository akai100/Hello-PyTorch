**transpose** 不是拷贝，而是改 stride**

## 调用 ```Tensor.t```方法实现转置

```python3
import torch

x = torch.randn(2, 3)
y = x.t()

x[0][0] = 0
```

```t()```实现矩阵转置，```y```不拷贝```x````的数据，共享同一份数据，只修改```stride````。修改```x```同时会修改```y```。

## 调用```Tensor.transpose```实现转置

```python3
def transpose(self, dim0: _int, dim1: _int) -> Tensor:
    pass
```

## 调用```torch.transpose```方法实现转置
