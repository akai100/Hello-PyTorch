## ```Tensor.pow```

对张量进行逐元素幂运算。

## ```Tensor.mean```

```python3
def mean(dim: _int | _size | None,
         keepdim: _bool = False
         *dtype: _dtype | None = None,
) -> Tensor:
     ....
```

+ dim

  指定计算平均值的维度。

+ keepdim

  是否保留原有的维度长度
